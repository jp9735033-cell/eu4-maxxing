country_decisions = {

	emx_restore_ming_dynasty = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = emx_ming_restored }
			NOT = { exists = MNG }
			OR = {
				tag = WUU
				tag = YUE
				primary_culture = jianghuai
				primary_culture = wu
				primary_culture = cantonese
				primary_culture = hakka
				primary_culture = min
			}
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
		}
		provinces_to_highlight = {
			if = {
				limit = { NOT = { tag = WUU } }
				OR = {
					province_id = 684		# Hangzhou
					province_id = 1821		# Nanjing
					province_id = 667		# Guangzhou
				}
				NOT = { owned_by = ROOT }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			if = {
				limit = { tag = WUU }
				mission_completed = emx_wu_four_pillars_united
			}
			else = {
				owns = 684		# Hangzhou
				owns = 1821		# Nanjing
				owns = 667		# Guangzhou
				1821 = { is_state = yes }
				total_development = 500
			}
		}
		effect = {
			if = { limit = { tag = WUU } set_country_flag = emx_origin_wu }
			if = { limit = { tag = YUE } set_country_flag = emx_origin_yue }
			change_tag = MNG
			swap_non_generic_missions = yes
			on_change_tag_effect = yes
			set_government_rank = 3
			set_country_flag = emx_ming_restored
			1821 = { move_capital_effect = yes }
			china_superregion = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = MNG
			}
			add_prestige = 25
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 3650
			}
			add_country_modifier = {
				name = "emx_unified_china"
				duration = -1
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 }
			}
		}
		ai_will_do = {
			factor = 100
		}
	}

}
