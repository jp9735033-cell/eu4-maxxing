country_decisions = {
	form_great_yuan = {
		major = yes
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			NOT = { has_country_flag = formed_yuan_flag }
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_free_or_tributary_trigger = yes
				ai = no
			}
			NOT = { exists = YUA }
			NOT = { tag = MUG }
			NOT = { tag = QNG }
			NOT = { tag = MNG }
			NOT = { tag = MCH }
			# eu4-maxxing: Shu is the only nation that may form Yuan, and the only
			# one the decision is visible to. Yan, Wu and Yue form Qing instead.
			tag = SHU
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 8
				}
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				province_id = 715	#Urumqi
				province_id = 2122	#Hami
				province_id = 2189	#Tsetserleg
				province_id = 2134	#Rebgong
				province_id = 4676	#West Alxa
				province_id = 4205	#Nagchu
				province_id = 4221	#Dundgovi
			}
			NOT = { owned_by = ROOT }
		}
		allow = {
			# eu4-maxxing: forming Yuan asks for administrative technology 10 and
			# seven provinces across the steppe and the plateau - nothing else.
			# No culture, no Beijing, no Mongol-provinces sweep, no stability or
			# peace requirement.
			adm_tech = 10
			owns = 715		#Urumqi
			owns = 2122		#Hami
			owns = 2189		#Tsetserleg
			owns = 2134		#Rebgong
			owns = 4676		#West Alxa
			owns = 4205		#Nagchu
			owns = 4221		#Dundgovi
		}
		effect = {
			if = {
				limit = { OR = { tag = YAN tag = SHU } }
				set_country_flag = han_yuan_restoration_flag
			}
			1816 = {
				add_base_tax = 1
				add_base_production = 1
				add_base_manpower = 1
				change_province_name = Khanbaliq
				rename_capital = Khanbaliq
				move_capital_effect = yes
			}
			723 = {
				change_province_name = Xanadu
				rename_capital = Xanadu 
			}
			2136 = {
				change_province_name = Zhongdu
				rename_capital = Zhongdu 
			}
			change_tag = YUA
			remove_non_electors_emperors_from_empire_effect = yes
			china_superregion = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = YUA
			}
			manchuria_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = YUA
			}
			mongolia_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = YUA
			}
			tibet_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = YUA
			}
			add_prestige = 20
			set_country_flag = formed_yuan_flag
			if = { #Sinicize the Mongols
				limit = {
					NOT = { has_global_flag = sinicized_altaic_flag }
					NOT = { has_country_flag = han_yuan_restoration_flag }
					culture_group = altaic
				}
				change_primary_culture = altaic_new
				set_ruler_culture = altaic_new
				if = {
					limit = {
						has_heir = yes
					}
					set_heir_culture = altaic_new
				}
				if = {
					limit = {
						has_consort = yes
						OR = {
							consort_culture = mongol
							consort_culture = oirats
							consort_culture = khalkha
							consort_culture = chahar
						}
					}
					set_consort_culture = altaic_new
				}
				hidden_effect = {
					every_province = {
						limit = {
							OR = {
								culture = mongol
								culture = oirats
								culture = khalkha
								culture = chahar
							}
						}
						change_culture = altaic_new
					}
					every_country = {
						limit = {
							OR = {
								primary_culture = mongol
								primary_culture = oirats
								primary_culture = khalkha
								primary_culture = chahar
							}
						}
						change_primary_culture = altaic_new
						set_ruler_culture = altaic_new
						if = {
							limit = {
								has_heir = yes
							}
							set_heir_culture = altaic_new
						}
						if = {
							limit = {
								has_consort = yes
								OR = {
									consort_culture = mongol
									consort_culture = oirats
									consort_culture = khalkha
									consort_culture = chahar
								}
							}
							set_consort_culture = altaic_new
						}
					}
					set_global_flag = sinicized_altaic_flag
					remove_accepted_culture = mongol
					remove_accepted_culture = oirats
					remove_accepted_culture = khalkha
					remove_accepted_culture = chahar
				}
				custom_tooltip = sinicize_mongols_ct
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			swap_non_generic_missions = yes
			on_change_tag_effect = yes
		}
		ai_will_do = {
			factor = 0
		}
		ai_importance = 400
	}
	hordes_restore_yuan = {
		potential = {
			has_country_flag = hordes_yuan_restoration
			NOT = { 
				YUA = {
					is_subject_of = ROOT
				}
			}
		}
		allow = {
			NOT = { exists = YUA }
			num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				OR = {
					region = mongolia_region
					region = north_china_region
					region = manchuria_region
					region = south_china_region
					region = xinan_region
				}
				value = 10
			}
		}
		effect = {
			custom_tooltip = HORDES_RESTORE_YUAN_EFFECT
			hidden_effect = {
				every_owned_province = {
					limit = {
						OR = {
							region = mongolia_region
							region = north_china_region
							region = manchuria_region
							region = south_china_region
							region = xinan_region
						}
					}
					add_core = YUA
					cede_province = YUA
				}
				
				create_subject = {
					subject_type = mongol_brother_realm
					subject = YUA
				}
				custom_tooltip = ""
				custom_tooltip = MONGOL_BROTHER_REALM_EXPLANATION
				
				YUA = {
					set_country_flag = yua_subject_less_liberty_desire_flag
				}
				
				clr_country_flag = hordes_yuan_restoration
			}
		}
		ai_will_do = {
			factor = 0
		}
	}
}
